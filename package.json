{
  "name": "wrangler-issue-repro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler pages dev build",
    "dev-dir": "wrangler pages dev build-dir",
    "build": "rm -rf ./build && mkdir -p ./build && cp ./src/my-worker.js ./build/_worker.js",
    "build-dir": "rm -rf ./build-dir && mkdir -p ./build-dir/_worker.js && cp ./src/my-worker.js ./build-dir/_worker.js/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wrangler": "^3.2.0"
  }
}
